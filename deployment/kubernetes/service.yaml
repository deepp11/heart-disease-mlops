apiVersion: v1
kind: Service
metadata:
  name: heart-disease-service
  labels:
    app: heart-disease-api
spec:
  selector:
    app: heart-disease-api
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  type: LoadBalancer  # Simulates cloud Load Balancer